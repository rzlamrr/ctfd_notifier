diff --git a/CTFd/plugins/dynamic_challenges/__init__.py b/CTFd/plugins/dynamic_challenges/__init__.py
index 45f51e38..11a959bc 100644
--- a/CTFd/plugins/dynamic_challenges/__init__.py
+++ b/CTFd/plugins/dynamic_challenges/__init__.py
@@ -9,6 +9,7 @@ from CTFd.plugins import register_plugin_assets_directory
 from CTFd.plugins.challenges import CHALLENGE_CLASSES, BaseChallenge
 from CTFd.plugins.dynamic_challenges.decay import DECAY_FUNCTIONS, logarithmic
 from CTFd.plugins.migrations import upgrade
+from CTFd.plugins.ctfd_notifier.hooks import wrap_dynamic_challenge_update, wrap_solve
 
 
 class DynamicChallenge(Challenges):
@@ -152,3 +153,15 @@ def load(app):
     register_plugin_assets_directory(
         app, base_path="/plugins/dynamic_challenges/assets/"
     )
+
+    # Let ctfd_notifier wrap dynamic update() once the class is registered
+    try:
+        wrap_dynamic_challenge_update(app)
+    except Exception:
+        app.logger.warning("ctfd_notifier: failed to wrap dynamic challenge update()", exc_info=True)
+
+    # Wrap dynamic solves separately so they also send notifications
+    try:
+        wrap_solve(DynamicValueChallenge)
+    except Exception:
+        app.logger.warning("ctfd_notifier: failed to wrap dynamic challenge solve()", exc_info=True)
